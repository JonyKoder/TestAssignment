
@page
@model IndexModel
@{
    ViewData["Title"] = "Company";
}

<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            text-align: left;
            padding: 8px;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <table>
        <thead>
            <tr>
                <th>№</th>
                <th>Company</th>
                <th>City</th>
                <th>State</th>
                <th>Phone</th>
            </tr>
        </thead>
       <tbody>
            @foreach (var company in Model.CompanyModel)
            {
                <tr>
                    <td>@company.Id</td>
                    <td><a href="/EditCompany?id=@company.Id">@company.Name</a></td>
                    <td>@company.City</td>
                    <td>@company.State</td>
                    <td>@company.Phone</td>
                </tr>
            }
       </tbody>
    
        <!-- Добавьте остальные строки таблицы -->
    </table>
    <script>
        function openEditModal(link) {
            var companyId = link.dataset.id;
            $('#editModal').modal('show');
            loadCompanyData(companyId);
        }

        function loadCompanyData(companyId) {
            // Assume here you have a function that retrieves company data using AJAX
            // and fills the form fields with the retrieved data
        }

        function saveCompanyChanges() {
            // Assume here you have a function that retrieves the form field values
            // and sends them to the server to save changes using AJAX
        }
    </script>
</body>
</html>

